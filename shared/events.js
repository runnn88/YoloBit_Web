const EVENTS = {
  SENSOR_READING_RECEIVED: "sensor.reading.received",
  DEVICE_STATE_CHANGED: "device.state.changed",
  CONTROL_COMMAND_REQUESTED: "control.command.requested",
};

module.exports = { EVENTS };
